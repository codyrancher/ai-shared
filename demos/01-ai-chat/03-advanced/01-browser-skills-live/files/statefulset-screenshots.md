Both screenshots look great:

- **stateful-list.png** - Shows the StatefulSet list page with the "web" StatefulSet (Active, 3/3 ready, nginx:1.25). The **Create** button in the top-right is highlighted with a red outline and glow.
- **stateful-detail.png** - Shows the detail page for `StatefulSet: web` with pod status (3 Running at 100%), resource info, insights, and the individual pods (web-0, web-1, web-2) all running.
